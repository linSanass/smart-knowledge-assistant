# 复制为项目根目录的 .env，供 docker compose 使用。
# 注意：这个文件和 backend/.env 不是同一个文件。
#
# backend/.env    -> 本机直接起后端时用（DATABASE_URL 指向 127.0.0.1）
# 根目录 .env     -> docker compose 用（DATABASE_URL 会被 compose 覆盖成服务名）

# MySQL root 密码。
# 不设置时 compose 用本地开发默认值 devpassword。
MYSQL_ROOT_PASSWORD=devpassword
